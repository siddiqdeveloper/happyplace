import { FoodDirective } from './food.directive';

describe('FoodDirective', () => {
  it('should create an instance', () => {
    const directive = new FoodDirective();
    expect(directive).toBeTruthy();
  });
});
